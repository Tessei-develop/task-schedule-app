generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Task {
  id                    String    @id @default(cuid())
  title                 String
  description           String?
  status                String    @default("TODO")
  priority              String    @default("MEDIUM")
  startDate             DateTime?
  dueDate               DateTime?
  completedAt           DateTime?
  estimatedMinutes      Int?
  startTime             String?
  endTime               String?
  tags                  String[]  @default([])
  recurrence            String?
  recurrenceInterval    Int?
  recurrenceEndDate     DateTime?
  // All occurrences in a recurrence series share the same seriesId so they
  // can be edited / queried as a group. Null = single (non-recurring) task.
  seriesId              String?
  googleCalendarEventId String?
  googleCalendarSynced  Boolean   @default(false)
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  @@index([seriesId])
}

model GoogleToken {
  id           String   @id @default("singleton")
  accessToken  String
  refreshToken String?
  expiresAt    DateTime
  scope        String
  tokenType    String
  syncToken    String?
  updatedAt    DateTime @updatedAt
}

model AIInsight {
  id        String   @id @default(cuid())
  type      String
  content   String
  date      String
  createdAt DateTime @default(now())
}
